@model IEnumerable<LeaveApplicationResponse>
@{ ViewBag.Title = "Leave Applications"; }

<div class="d-flex justify-content-between align-items-center mb-4 mt-2">
    <div>
<<<<<<< HEAD
        <h2 class="fw-bold mb-0 text-dark">Leave Applications</h2>
=======
        <h2 class="fw-bold mb-0 ">Leave Applications</h2>
>>>>>>> main
        <p class="text-muted mb-0">Track requests, durations, and approval status</p>
    </div>
    <a asp-action="Create" class="btn btn-primary btn-primary-elevated px-4 py-2">
        <i class="fa-solid fa-plus me-2"></i>New Application
    </a>
</div>

@if (TempData["WorkflowSuccess"] is string workflowSuccess && !string.IsNullOrWhiteSpace(workflowSuccess))
{
<<<<<<< HEAD
    <div class="alert alert-success status-message shadow-sm border-0 rounded-4 mb-4" role="status" aria-live="polite">
        <i class="fa-solid fa-circle-check"></i>
        <div>@workflowSuccess</div>
=======
    <div class="alert alert-success shadow-sm border-0 rounded-4 mb-4" role="alert">
        <i class="fa-solid fa-circle-check me-2"></i>@workflowSuccess
>>>>>>> main
    </div>
}

@if (TempData["WorkflowError"] is string workflowError && !string.IsNullOrWhiteSpace(workflowError))
{
<<<<<<< HEAD
    <div class="alert alert-danger status-message shadow-sm border-0 rounded-4 mb-4" role="alert">
        <i class="fa-solid fa-triangle-exclamation"></i>
        <div>@workflowError</div>
=======
    <div class="alert alert-danger shadow-sm border-0 rounded-4 mb-4" role="alert">
        <i class="fa-solid fa-triangle-exclamation me-2"></i>@workflowError
>>>>>>> main
    </div>
}

@if (!Model.Any())
{
    <div class="alert alert-light text-center p-5 shadow-sm rounded-4 border-0" role="alert">
<<<<<<< HEAD
        <i class="fa-solid fa-calendar-xmark text-muted mb-3 empty-state-icon"></i>
        <h4 class="text-dark fw-bold">No Leave Applications Found</h4>
        <p class="text-muted mb-4">Start by creating your first leave application.</p>
        <a asp-action="Create" class="btn btn-primary px-4 py-2 rounded-3"><i class="fa-solid fa-plus me-2"></i>Add Application</a>
=======
        <h4 class=" fw-bold">No Leave Applications Found</h4>
        <p class="text-muted mb-4">Start by creating your first leave application.</p>
        <a asp-action="Create" class="btn btn-primary px-4 py-2 rounded-3">Add Application</a>
>>>>>>> main
    </div>
}
else
{
    <div class="card list-card mb-5">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h3 class="card-title fw-bold m-0"><i class="fa-solid fa-list-check me-2"></i> Application Analysis</h3>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover table-modern mb-0 align-middle js-data-table">
<<<<<<< HEAD
                    <thead class="table-light text-muted">
=======
                    <thead class="border-1 text-muted">
>>>>>>> main
                        <tr>
                            <th class="ps-4 text-uppercase border-0 table-head-label">Employee</th>
                            <th class="text-uppercase border-0 table-head-label">Type</th>
                            <th class="text-uppercase border-0 table-head-label">Duration</th>
                            <th class="text-uppercase border-0 table-head-label">Period</th>
                            <th class="text-uppercase border-0 table-head-label">Days</th>
                            <th class="text-uppercase border-0 table-head-label">Status</th>
                            <th class="text-center text-uppercase border-0 table-head-label">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="border-top-0">
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-4 fw-semibold">@item.EmployeeName</td>
                                <td>@item.LeaveTypeName</td>
                                <td>@item.DurationName</td>
                                <td>@item.StartDate.ToString("dd MMM yyyy") - @item.EndDate.ToString("dd MMM yyyy")</td>
                                <td><span class="badge bg-secondary">@item.Days day(s)</span></td>
                                <td><span class="badge @GetStatusBadge(item.Status)">@item.Status</span></td>
                                <td>
                                    <div class="d-flex gap-2 justify-content-center">
                                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info action-btn" data-bs-toggle="tooltip" title="View Details">
                                            <i class="fa-solid fa-eye"></i>
                                        </a>
                                        @if (IsPendingStatus(item.Status))
                                        {
                                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-warning action-btn" data-bs-toggle="tooltip" title="Edit">
                                                <i class="fa-solid fa-pen"></i>
                                            </a>
                                            <div class="dropdown">
                                                <button
                                                    class="btn btn-sm btn-outline-primary action-btn dropdown-toggle"
                                                    type="button"
                                                    data-bs-toggle="dropdown"
                                                    aria-expanded="false"
                                                    title="Workflow Actions"
                                                >
                                                    <i class="fa-solid fa-bolt"></i>
                                                </button>
                                                <div class="dropdown-menu dropdown-menu-end shadow-sm">
                                                    <form asp-action="Approve" asp-route-id="@item.Id" method="post">
                                                        @Html.AntiForgeryToken()
                                                        <button type="submit" class="dropdown-item text-success">
                                                            <i class="fa-solid fa-check me-2"></i>Approve
                                                        </button>
                                                    </form>

                                                    <a asp-action="Reject" asp-route-id="@item.Id" class="dropdown-item text-danger">
                                                        <i class="fa-solid fa-xmark me-2"></i>Reject
                                                    </a>

                                                    <form asp-action="Cancel" asp-route-id="@item.Id" method="post">
                                                        @Html.AntiForgeryToken()
                                                        <button type="submit" class="dropdown-item text-secondary">
                                                            <i class="fa-solid fa-ban me-2"></i>Cancel
                                                        </button>
                                                    </form>
                                                </div>
                                            </div>

                                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger action-btn" data-bs-toggle="tooltip" title="Delete">
                                                <i class="fa-solid fa-trash"></i>
                                            </a>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}

@functions {
    private static string GetStatusBadge(string status) =>
        status.ToLowerInvariant() switch
        {
            var s when s.Contains("approve") => "bg-success",
            var s when s.Contains("cancel") => "bg-secondary",
            var s when s.Contains("reject") => "bg-danger",
<<<<<<< HEAD
            var s when s.Contains("pending") => "bg-warning text-dark",
=======
            var s when s.Contains("pending") => "bg-warning ",
>>>>>>> main
            _ => "bg-secondary"
        };

    private static bool IsPendingStatus(string status) =>
        !string.IsNullOrWhiteSpace(status)
        && status.Contains("pending", StringComparison.OrdinalIgnoreCase);
}

@section scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
            tooltipTriggerList.map(function (tooltipTriggerEl) {
                return new bootstrap.Tooltip(tooltipTriggerEl);
            });
        });
    </script>
}
