@model ResetPasswordRequest
@{
    ViewData["Title"] = "Reset Your Password";
}

<div class="row justify-content-center">
    <div class="col-lg-6 col-xl-5">
        <div class="text-center mb-4">
            <h1 class="fw-bold">@ViewData["Title"]</h1>
            <p class="text-muted">Create a new, strong password for your account.</p>
        </div>

        <div class="card shadow-sm">
            <div class="card-body p-4 p-lg-5">
                <h4 class="card-title text-center fw-semibold mb-4"><i class="bi bi-key-fill me-2"></i>Set New Password</h4>
                <form asp-action="ResetPassword" method="post">
                    <input asp-for="Email" type="hidden" />
                    <input asp-for="Code" type="hidden" />
                    <div asp-validation-summary="ModelOnly" class="text-danger alert alert-danger p-2 mb-3"></div>

                    <div class="form-floating mb-3">
                        <input asp-for="Password" class="form-control" placeholder="New Password" autocomplete="new-password" />
                        <label asp-for="Password"></label>
                        <span asp-validation-for="Password" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="form-floating mb-3">
                        <input asp-for="ConfirmPassword" class="form-control" placeholder="Confirm New Password" autocomplete="new-password" />
                        <label asp-for="ConfirmPassword"></label>
                        <span asp-validation-for="ConfirmPassword" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="d-grid mt-4">
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="bi bi-check-lg me-2"></i>Reset Password
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
